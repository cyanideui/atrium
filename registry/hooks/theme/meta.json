{
  "name": "hook-theme",
  "category": "hooks",
  "title": "useTheme",
  "description": "Light/dark theme state backed by a module-level external store (useSyncExternalStore), so every call site shares one value. Persists to localStorage, applies `.dark` on <html>.",
  "version": "1.0.0",
  "frameworks": ["next", "vite-react-router"],
  "files": [
    {
      "from": "use-theme.ts",
      "to": "src/hooks/use-theme.ts"
    }
  ],
  "dependencies": {
    "npm": ["react"]
  },
  "postInstall": [
    "Call useTheme() anywhere. Wire a T hotkey if you want keyboard toggling (see the doc-shell starter)."
  ]
}
