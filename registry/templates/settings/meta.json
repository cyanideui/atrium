{
  "name": "template-settings",
  "category": "templates",
  "title": "Settings",
  "description": "Tabbed sectioned form with autosave — Profile / Notifications / Security / Billing. Drop-in for /settings, /account, /workspace/preferences.",
  "version": "1.0.0",
  "frameworks": ["next", "vite-react-router"],
  "files": [
    {
      "from": "settings.tsx",
      "to": "src/pages/settings.tsx"
    }
  ],
  "dependencies": {
    "npm": ["@atrium/ui", "@hugeicons/core-free-icons", "@hugeicons/react", "react"]
  },
  "postInstall": [
    "Replace the mock autosave effect with your real mutation/save logic.",
    "Next.js: move to src/app/settings/page.tsx. Vite: route it via <Route element={<SettingsPage />} />."
  ]
}
